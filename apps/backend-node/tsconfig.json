{
  "extends": "@repo/typescript-config/base.json",
  "compilerOptions": {
    /* Set to NodeNext for modern Node.js ESM support */

    /* Since a bundler will do the actual building, tsc is just for type-checking */
    "noEmit": true,

    "outDir": "./dist",
    "types": ["node"],
  },
  "include": ["src/**/*", "api/**/*"],
  "exclude": ["node_modules", "dist"],
}
